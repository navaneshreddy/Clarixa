{
  "name": "clarixa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-with-qr.mjs",
    "dev:local": "vite",
    "dev:qr": "node scripts/dev-with-qr.mjs",
    "build": "tsc -b && vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "firebase": "^12.12.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.36.3",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^5.0.0",
    "qrcode-terminal": "^0.12.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "wrangler": "^4.90.0"
  }
}
